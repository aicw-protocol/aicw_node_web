/** Base globe mesh radius used by instanced node markers. */
export const GLOBE_RADIUS = 1;

/** Slightly larger sphere for the atmospheric glow shader. */
export const GLOBE_ATMOSPHERE_RADIUS = 1.03;

/** Marker placement sits just above the surface to avoid z-fighting. */
export const GLOBE_MARKER_RADIUS = 1.015;
